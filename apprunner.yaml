version: 1.0
auto-deployments-enabled: true
runtime: custom
build:
  commands:
    build:
    - docker build -t proxy-app .
run:
  command: ./proxy
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
